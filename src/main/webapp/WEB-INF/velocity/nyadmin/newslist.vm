#set($pageJS='
<script>
$(document).ready(function(){
    $(".sidebar-menu li.treeview>a:contains(新闻管理)").parent().addClass("active");
    $("#sortbox").change(function(){
        var value=$(this).children("option:selected").val();
        $.get("/nyadmin/newslistbysort/"+value,function(data){

            var newslist=data;
            $.each(newslist,function(i,news)
            {
                var name=news.newsSort.name;
                var title=news.title;
                var date=news.addDate;
                var id=news.id;
                $("#newslistbysort").html("<tr><td>"+name+"</td><td>"+title+"</td><td>"+date+"</td></td> </tr>");
            })
        },"json")
    });
});
</script>')
<section class="content-header">
    <h1>
        新闻管理
        <small>文章管理</small>
    </h1>

</section>

<!-- Main content -->
<section class="content">
    <div class="row">
        <div class="col-xs-12">
            <div class="box">
                <div class="box-header">
                    <h3 class="box-title">文章列表 </h3>
                    <a class="pull-right hidden-xs">类别
                        <select id="sortbox">
                            #foreach($newsSort in $newsSortList)
                                #if($newsSort.id!=1)
                                    <option value="$newsSort.id">$newsSort.name</option>
                                #end
                            #end
                        </select>
                    </a>
                </div>
                <!-- /.box-header -->
                <div class="box-body">
                    <table id="example2" class="table table-bordered table-hover">
                        <thead>
                        <tr>
                            <th>类别</th>
                            <th>标题</th>
                            <th>添加时间</th>
                            <th>操作</th>
                        </tr>
                        </thead>
                        <tbody id="newslistbysort">

                            #foreach($news in $newsList)
                            <tr>
                                <td>$news.newsSort.name</td>
                                <td>$news.title</td>
                                <td>$news.addDate</td>
                                <td><a href="newseditpage?id=$news.id">修改 </a><a href="newsdelete?id=$news.id">删除</a>
                                </td>
                            </tr>
                            #end


                        </tfoot>
                    </table>
                </div>
                <!-- /.box-body -->
            </div>
            <!-- /.box -->
        </div>
        <!-- /.col -->
    </div>
    <!-- /.row -->
</section><!-- /.content -->

